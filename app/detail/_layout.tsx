import {Stack} from "expo-router";
import React from "react";

export default function Layout() {
    return (<Stack>
        {/*<Stack.Screen options={{headerTitle: `Details `}}*/}
        {/*/>*/}
        {/*<Stack.Screen name={'[id]'} options={{headerTitle:"id",}}/>*/}
    </Stack>);}
